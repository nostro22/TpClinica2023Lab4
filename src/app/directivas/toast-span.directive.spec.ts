import { ToastSpanDirective } from './toast-span.directive';

describe('ToastSpanDirective', () => {
  it('should create an instance', () => {
    const directive = new ToastSpanDirective();
    expect(directive).toBeTruthy();
  });
});
