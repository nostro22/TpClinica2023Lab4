import { AlertSpanDirective } from './alert-span.directive';

describe('AlertSpanDirective', () => {
  it('should create an instance', () => {
    const directive = new AlertSpanDirective();
    expect(directive).toBeTruthy();
  });
});
