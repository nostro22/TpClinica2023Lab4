import { PonerTodoMayusculaPipe } from './poner-todo-mayuscula.pipe';

describe('PonerTodoMayusculaPipe', () => {
  it('create an instance', () => {
    const pipe = new PonerTodoMayusculaPipe();
    expect(pipe).toBeTruthy();
  });
});
