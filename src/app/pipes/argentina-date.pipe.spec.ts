import { ArgentinaDatePipe } from './argentina-date.pipe';

describe('ArgentinaDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ArgentinaDatePipe();
    expect(pipe).toBeTruthy();
  });
});
